{
  "name": "nextsample3-root",
  "private": true,
  "scripts": {
    "dev": "cd my-next-project && npm run dev",
    "build": "npm run vercel-build",
    "vercel-build": "cd my-next-project && npm ci && npm run build"
  },
  "dependencies": {
    "dayjs": "^1.11.10",
    "microcms-js-sdk": "^3.1.0",
    "nextjs-basic-auth-middleware": "^3.1.1"
  },
  "devDependencies": {
    "next": "^14.1.4"
  }
}
